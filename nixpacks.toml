[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate && npm run build"]

[start]
cmd = "npx prisma migrate deploy && npx prisma db seed && npm start"
